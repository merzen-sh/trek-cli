name: release-wip

on:
  workflow_dispatch:
    inputs:
      increment:
        description: "SemVer Increment"
        required: true
        default: "prerelease"
        type: choice
        options:
          - prerelease
          - prepatch
          - preminor
          - premajor
          - patch
          - minor
          - major
      releaseType:
        description: "Release type"
        required: true
        type: choice
        options:
          - latest
          - canary

permissions:
  contents: write
  pull-requests: write

jobs:
  bump-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      release-tag: ${{ steps.version.outputs.release-tag }}
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-tags: true
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: false

      - name: install
        run: pnpm install --frozen-lockfile

      - name: version
        id: version
        run: |
          node scripts/compute-version.js "${{ inputs.increment }}" "${{ inputs.releaseType }}"
          VERSION=$(cat version | head -n 1)
          RELEASE_TAG=$(cat version | tail -n 1)
          echo "   DEBUG: version = $VERSION"
          echo "   DEBUG: release-tag = $RELEASE_TAG"

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "releaseTag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"

      - name: bump version
        run: |
          ./scripts/bump-version.sh "${{ steps.version.outputs.version }}"

      - name: Generate Changelog
        uses: orhun/git-cliff-action@v4
        id: git-cliff
        with:
          config: cliff.toml
          args: --tag ${{ steps.version.outputs.releaseTag }}
        env:
          OUTPUT: CHANGELOG.md
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create pull request
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "release: bump version to ${{ steps.version.outputs.version }}"
          title: "release: v${{ steps.version.outputs.version }}"
          body-path: ./CHANGELOG.md
          base: main
          branch: release/v${{ steps.version.outputs.release-tag }}

  build-app:
    needs: bump-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: false

      - name: install
        run: pnpm install --frozen-lockfile

      - name: build app
        run: pnpm --filter app build

      - name: upload app build
        uses: actions/upload-artifact@v7
        with:
          name: app-dist
          path: packages/app/dist/

  build-rust:
    needs: [bump-version, build-app]
    strategy:
      matrix:
        settings:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu

          - os: windows-latest
            target: x86_64-pc-windows-msvc

    runs-on: ${{ matrix.settings.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Download app artifact
        uses: actions/download-artifact@v7
        with:
          name: app-dist
          path: packages/app/dist/

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.96.0
          targets: ${{ matrix.settings.target }}

      - name: Build rust
        run: cargo build --target ${{ matrix.settings.target }} -p trek --release

      - name: Binary rename
        run: |
          mv target/${{ matrix.settings.target }}/release/trek target/${{ matrix.settings.target }}/release/trek-${{ matrix.settings.target }}

      - name: upload trek-cli
        uses: actions/upload-artifact@v7
        with:
          name: trek-${{ matrix.settings.target }}
          path: target/${{ matrix.settings.target }}/release/trek-${{ matrix.settings.target }}

  release:
    needs: [build-rust, bump-version]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download artifacts
        uses: actions/download-artifact@v7
        with:
          name: trek-*
          path: ./binaries/

      - name: compress binaries for each platform
        run: ./scripts/prepare-tarball.sh

      - name: generate SHASUMS.txt
        run: ./scripts/generate-checksums.sh

      - name: release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.bump-version.outputs.release-tag }}
          release_name: Trek CLI v${{ needs.bump-version.outputs.release-tag }}
          draft: false
          prerelease: ${{ inputs.releaseType == 'canary'}}
          asset_path: ./dist
